import React from 'react';
import styles from './Dashboard.module.scss';

const Dashboard = () => {
  return (
    <div>
      <h2 className={styles.component}>Dashboard</h2>
    </div>
  );
};

export default Dashboard;